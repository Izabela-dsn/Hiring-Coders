// tsc --init (cria um arquivo que
//  configura o typescript)

//tsc -> gera o arquivo js

// TS
function sumT(num1: number, num2: number) {
  return num1 + num2
}
const num1 = 10
const num2 = 5
console.log(sumT(num1, num2))
